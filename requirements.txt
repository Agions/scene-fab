# Voxplore 依赖列表
# AI 视频解说工具

# 核心依赖
numpy>=1.21.0
Pillow>=9.0.0

# 视频处理
opencv-python>=4.5.0
opencv-python-headless>=4.5.0
ffmpeg-python>=0.2.0
decord>=0.6.0  # 快速视频帧提取，比 OpenCV 快 2-3 倍

# AI 服务
requests>=2.28.0

# 性能优化推荐
orjson>=3.9.0  # JSON 序列化加速 5-10 倍，比标准 json 快
psutil>=5.9.0  # 系统资源监控

# LLM/视觉 (按需安装)
# pip install openai      # OpenAI GPT
# pip install dashscope   # 阿里云百炼

# 语音识别 (按需安装)
# pip install faster-whisper

# TTS (按需安装)
# pip install edge-tts

# 音频处理
librosa>=0.9.0

# 配置管理
PyYAML>=6.0
python-dotenv>=1.0.0

# Web 服务 (可选)
# fastapi>=0.100.0
# uvicorn>=0.23.0

# 数据科学
scipy>=1.9.0

# 进度条
tqdm>=4.65.0

# 开发/测试
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
